version: '3'

services:
  map:
    image: mhajder/openstreetmap-tile-server-cyclosm
    volumes:
      - osm-data:/data/database/
    ports:
      - "8080:80"
    command: "run"

volumes:
  osm-data:
    external: true
